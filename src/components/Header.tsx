import { Center, Heading } from "@chakra-ui/react";
import React from "react";

export const Header = () => {
  return (
    <Center p={100}>
      <Heading>Get things done</Heading>
    </Center>
  );
};
